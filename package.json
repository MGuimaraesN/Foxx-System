{
  "name": "Foxx-System",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "prisma:dev": "npm run prisma:dev --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "prisma:start": "npm run prisma:start --prefix backend",
    "start": "concurrently -n backend,frontend \"npm --prefix backend run start\" \"npm --prefix frontend run preview\"",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
