services:
  backend:
    build: ./backend
    container_name: MGN-System-api
    restart: always
    ports:
      - "3020:3020"
    volumes:
      # Persistência do banco de dados SQLite
      - sqlite_data:/app

  frontend:
    build: ./frontend
    container_name: MGN-System-web
    restart: always
    ports:
      - "3000:80" # Acessar no navegador via localhost:3000
    depends_on:
      - backend

volumes:
  sqlite_data:
